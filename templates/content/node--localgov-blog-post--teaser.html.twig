{#
  The only reason we need this file is because it's defined in the
  news module, so we need this to override the one that comes with
  that module.
#}

{% include "node--teaser.html.twig" with {
  main_image: content.localgov_blog_image
} %}
